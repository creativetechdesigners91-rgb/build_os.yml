name: Build Aevox OS (build_os.yml Edition)

on:
  workflow_dispatch:

jobs:
  build-aevox-light:
    runs-on: ubuntu-24.04
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Free Disk Space
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" /usr/share/swift
        sudo docker image prune --all --force
        df -h

    - name: Configure 32-Bit Architecture & Legacy Repos
      # Dynamically maps legacy Ubuntu archives into Ubuntu 24.04 for structural AOSP compatibility
      run: |
        sudo dpkg --add-architecture i386
        echo "deb http://ubuntu.com focal-security main universe" | sudo tee /etc/apt/sources.list.d/focal-security.list
        sudo apt-get update

    - name: Install Android Dependencies (Ubuntu 24.04 Fixed)
      run: |
        sudo apt-get update
        # Replaced modern missing packets with localized version-mapped equivalents
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib \
          gcc-multilib git gnupg gperf lib32readline-dev \
          lib32z1-dev liblz4-tool libncurses-dev libsdl1.2-dev \
          libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool \
          squashfs-tools xsltproc zip zlib1g-dev xorriso genisoimage \
          libncurses5:i386 libncurses5 libtinfo5:i386 libtinfo5

    - name: Configure GitHub Actions Compiler Ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ccache-aevox-light-${{ runner.os }}-${{ github.sha }}
        restore-keys: ccache-aevox-light-${{ runner.os }}-

    - name: Configure Google Repo Tool
      run: |
        mkdir -p ~/bin
        curl https://googleapis.com > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "$HOME/bin" >> $GITHUB_PATH

    - name: Download Android Source (Shallow Clone)
      run: |
        git config --global user.name "Aevox OS Developer"
        git config --global user.email "developer@aevox.internal"
        repo init -u https://osdn.net -b android-9.0-r2 --depth=1
        repo sync -c -j$(nproc) --no-tags --no-clone-bundle --force-sync

    - name: Strip Android Bloatware & Inject Light Optimizations
      run: |
        sed -i 's/Android-x86/Aevox OS/g' bootable/newinstaller/initrd/init
        sed -i 's/Android-x86/Aevox OS/g' bootable/newinstaller/install.sh
        
        rm -rf packages/apps/WallpaperPicker
        rm -rf packages/apps/LiveWallpapersPicker
        rm -rf packages/apps/Gallery2
        rm -rf packages/apps/Music
        rm -rf packages/apps/Calendar
        rm -rf packages/apps/Email
        rm -rf packages/services/BuiltInPrintService
        
        mkdir -p vendor/aevox/overlay/frameworks/base/core/res/res/values
        cat <<EOF > vendor/aevox/overlay/frameworks/base/core/res/res/values/config.xml
        <resources>
            <bool name="config_enableHardwareAcceleration">true</bool>
            <integer name="config_longPressOnHomeBehavior">0</integer>
            <string name="config_defaultWallpaperComponent">none</string>
            <bool name="config_disableTransitionAnimation">true</bool>
            <bool name="config_hasSystemApps">false</bool>
        </resources>
        EOF

    - name: Compile Ultra-Light Android ISO
      run: |
        export USE_CCACHE=1
        export CCACHE_DIR=~/.ccache
        ccache -M 30G
        
        source build/envsetup.sh
        lunch android_x86_64-user
        
        export ISO_CMDS="mksquashfs -comp lzo"
        m -j$(nproc) iso_img
        
        mv out/target/product/x86_64/*.iso out/target/product/x86_64/aevox-os-light.iso

    - name: Upload Finished Aevox OS ISO
      uses: actions/upload-artifact@v4
      with:
        name: Aevox-OS-Light-ISO
        path: out/target/product/x86_64/aevox-os-light.iso
        if-no-files-found: error
        retention-days: 3
